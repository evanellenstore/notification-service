server:
  port: 2020

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost:3306/notification_db
    username: root
    password: ellenverma

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
