spring:
  datasource:
    url: jdbc:mysql://mysql.store.svc.cluster.local:3306/store_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 2011
